Use $modular-figure-design to draw this method:

Retrieved passages join the query before answer generation.
- query → retrieve: query.
- retrieve → generate: passages.
- generate → answer: answer.
- query → generate: query bypass.

Write the plan and edge specification first. Use restrained geometry and editable labels. Deliver SVG, PNG, PDF and module crops. Mark all examples as illustrative.

Production note: these repository examples were authored as SVG geometry; no image generation model was used.
