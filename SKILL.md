---
name: modular-figure-design
description: "Create rigorous CVPR-style paper method figures and framework diagrams by planning semantic relationships, generating visual modules separately, and composing precise typography, formulas, and arrows. Use for plan-first modular scientific figures and refinement of research architecture diagrams. General across research domains, but not intended for cartoon illustrations, ordinary slide decks, or standalone numerical plots."
---

# CVPR 模块化论文图

**领域通用，风格明确：严谨的科研方法图。** 先定义图要证明或解释的关系，再拆分视觉模块、分别制作，最后统一排版和精确连线。不要一口气生成整张复杂图并接受其随机排版。

## 风格与适用边界

- 默认采用 CVPR 论文式的真实样例＋简化几何＋克制配色＋清楚连线。没有唯一的CVPR画风，按用户参考图落实密度、字体、层级和线条。
- 排除卡通小孩、可爱人物、玩具机器人、拟人化大脑、装饰齿轮、办公场景、插画海报和PPT贴纸风。不要把“精致”解释为增加装饰。
- 不默认使用老师/学生等人物隐喻。研究中的teacher/student模型若确实存在，按模型或算法节点表达；不要因为名字而画人物。
- 不绑定特定研究领域、固定模块数、三栏布局、蓝色箭头或循环。不同方法复用制作流程，不复用同一套业务逻辑。
- 简单改字直接修改已认可源稿；普通数值图用科学绘图库。用户明确要完全不同风格时，遵循新要求，不强套本技能。

## 1. 先定叙事和事实

提炼一句核心命题：读者看完后必须理解哪个机制、因果关系或差异？用它决定主次。

提取实体、输入输出、状态、控制与选择条件。区分已实现行为、拟议协议、真实样例和示意内容；不发明指标、实验曲线、真实错例、神经网络层或反馈回路。

参考图用于提取视觉规则，不用于复制其方法或结果。用户已认可的结构是迭代基准；记录本轮改动和必须保留项。

只有缺失信息会改变图的含义时才提问。已有信息足够就继续，将假设写入制作记录或图注。

## 2. 写图稿计划再执行

读取 [references/planning.md](references/planning.md)，在当前任务输出目录保存 `plan.md` 和 `figure-spec.json`。向用户简要说明主线、模块分工和制作顺序。

计划明确：核心命题、用途及阅读尺寸；模块的主要动作/输入/输出；每条边的起点/终点/载荷；版式和走线通道；原始素材、需生成素材与精确排版元素；验收条件。

**按plan制作不是自动增加审批。** 用户只要计划时停在计划；已要求完成图时，计划后继续制作。不因使用本技能擅自创建目标、任务、子Agent或外部发布。

## 3. 分模块制作

读取 [references/prompting.md](references/prompting.md)。每个视觉模块承担一个主要动作，避免每块都变成小海报。

- 真实数据与实验结果使用可追溯原件，不用生成图片冒充真实证据。
- 手写变化、视觉变换、复杂科学示意等适合图像生成的素材，使用可用内置图像工具分别生成。依当前工具规范执行；不擅自切换外部API或付费模型。
- 精确几何、矩阵、树、公式、标签、图例和跨模块箭头由可编辑排版层负责。科学数值图使用标准绘图库，不由图像模型编造。
- 并非每块都必须调用ImageGen；精确几何图直接绘制。用户明确要求生成式素材时，不能仅用空框和公式充数。
- 保持模块间一致的线条、配色、视角和样例处理。生成后先看模块，发现语义或文字错误就局部修正。

跨模块箭头、编号和复杂公式留到合版；生成模型输出的文字不能直接视为正确。

## 4. 统一合版与连线

读取 [references/composition.md](references/composition.md)。统一字体、线宽、间距、画布与素材处理。

- 主贡献占据主要视觉面积，其余模块保留理解它所需的证据。
- 上层直观现象、下层形式机制可以并存，但不能重复整条因果链或同一组输出。
- 不用空框、公式、装饰网络图或密集文字撑版面。
- **依据渲染后的端口位置连线。** 每条箭头必须对应规格里的输入输出，不能落在附近空白或过期坐标上。
- 主关系用一种明显强调方式，局部依赖弱化。多种线型确有必要时给出图例，颜色不能只是装饰。
- 不穿过不相关模块、文字和图像。分支合流与反馈对象须符合方法；布局变化后重算坐标。

## 5. 验收与局部迭代

先检查语义：主线可追踪，每条边的载荷和方向正确，条件分支/返回对象正确，真实证据与示意可区分。

再看整图、目标阅读尺寸和关键局部：字号、层级、裁切、留白、重叠、端点。网页无溢出不等于论文图可读。

HTML/SVG工作流可使用 `scripts/render_review.cjs` 导出图和独立模块，检查标注过的连接端口及主线穿字。其他绘图方式应做等效检查。机械检查不替代视觉和语义审查。

按反馈区分语义、版式、素材或文字问题，局部修改并重验相关连接。用户认可后不无故重设计。多候选只有在用户需要时制作，候选应在叙事或布局上不同，而非仅换颜色。

## 6. 交付

给最终预览、使用文件和关键改动，不自称“顶会级”或给自己打分。

通常保存总图PNG、按需求导出的PDF和可编辑源文件；模块化任务同时保留独立模块、计划、规格、图注、提示词及素材来源。生成方式和提示词按工具要求报告。

混合位图和矢量排版时如实说明；嵌入PNG的PDF不等于全矢量插图。用户只要图时，不额外搭网站、做PPT或重写整篇报告。

## 可复用示例

需要具体的输入、计划和交付参照时，查看 [examples/workflow](examples/workflow/)、[examples/retrieval](examples/retrieval/) 或 [examples/distillation](examples/distillation/)。这些是不同关系结构的示意，不是固定版式，也不代表实验结果。
